WebServer处理客户端请求总共分为三大步
1:解析请求
2:处理请求
3:响应客户端

本版本开始完成第一步:解析请求工作
通过了解HTTP协议对请求Request(客户端发送服务端的内容)得知,
一个有三部分组成
1:请求行,2:消息头,3:消息正文
并且请求行与消息头都是文本内容,对应的字符集:ISO8859-1,而且特点是以CR,LF结尾(回车,换行)
正文部分是二进制数据,为用户提交内容

因此,本版本先做一个测试,完成读取客户端发送过来的一行字符串操作(以CR,LF结尾的一行内容)
因为请求行和消息头都是采用这个方式来读取