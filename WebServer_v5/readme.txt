此版本开始完成第三步:响应客户端的工作

上一个版本中,ClientHandler根据请求中的抽象路径去
webapps目录里寻找用户请求的资源,并以分支的形式分别打桩
此版本先来处理找到资源的情况,我们要讲用户请求的页面发送回
客户端

注意:
服务器要想给客户端发消息也要遵循HTTP协议的要求
发送一个响应(HttpResponse)内容
而响应内容的具体格式参考文档或者http.txt