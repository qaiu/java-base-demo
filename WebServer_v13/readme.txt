此版本開始完成用戶註冊功能

上一版本中,我們已經讓HttpRequest支持了不同的
請求方式(帶參數和不帶參數).

因此本版本我们完成处理注册业务

首先我们要修改ClientHandler的处理请求环节
因为我们已经不能再用HttpRequest的URI作为请求路径进行判断了(因为URI可能含有参数部分)
我们要改为使用RequestURI的值,这个值是表示请求部分的

这里要做一个分支,首先看请求路径是否为请求一个业务,这里以注册为例
注册页面form表单我们指定为requestURI的值就是
/myweb/reg,因此我们要只对这个值做操作
如果不是再走以前的逻辑
判断是否为请求一个静态资源.